repo: git@github.com:juju-solutions/layer-nvidia-cuda
includes: ['layer:basic']  # if you use any interfaces, add them here
options:
  basic:
    packages:
      - git
      - build-essential
      - software-properties-common
