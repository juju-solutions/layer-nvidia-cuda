includes: ['layer:basic']  # if you use any interfaces, add them here
options:
  basic:
    packages:
      - git
      - libopenblas-base
      - libopenblas-dev
      - build-essential
      - software-properties-common