name: cuda
summary: installs CUDA drivers on GPU enabled instances
maintainer: Samuel Cozannet <samnco@gmail.com>
description: |
  Installs CUDA drivers to instance
tags:
  - misc
subordinate: true
provides:
  cuda-driver:
    interface: cuda
requires:
  juju-info:
    interface: juju-info
    scope: container
